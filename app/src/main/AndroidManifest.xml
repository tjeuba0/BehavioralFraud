<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- TASK-026 / FR-CL-11 REQ-01: required to read last-known location's
         isFromMockProvider() flag for GPS-spoofing fraud signal. Used
         READ-ONLY (never requestLocationUpdates). Collector degrades
         gracefully if user denies — feature returns false. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="Behavioral Fraud POC"
        android:supportsRtl="true"
        android:theme="@style/Theme.BehavioralFraudPOC"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BehavioralFraudPOC">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
